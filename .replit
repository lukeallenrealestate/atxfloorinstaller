run = "python3 -m http.server 8080 --directory ."
entrypoint = "index.html"

[deployment]
run = ["sh", "-c", "python3 -m http.server 8080 --directory ."]
deploymentTarget = "static"

[[ports]]
localPort = 8080
externalPort = 80

[agent]
expertMode = true
